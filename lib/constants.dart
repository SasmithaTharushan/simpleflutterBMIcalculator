import 'package:flutter/material.dart';

const kTextColor = Color(0xFFFF8888);
const kInputLabelColor =  TextStyle(
                              color: kTextColor, // Text color
                                //style: TextStyle(color: Colors(#672677)),), can define as a hex code too
                              fontSize: 50, // Set a font size (adjust as needed)
                              fontWeight: FontWeight.bold, // Optional: Make the text bold 
                              );

                              
const kTextColor1 = Color(0xFF32746D);
const kOutputLabelColor =  TextStyle(
                              color: kTextColor1, // Text color
                                //style: TextStyle(color: Colors(#672677)),), can define as a hex code too
                              fontSize: 60, // Set a font size (adjust as needed)
                              fontWeight: FontWeight.bold, // Optional: Make the text bold 
                              );

